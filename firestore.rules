rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function groupIsOpen(groupId) {
      return get(/databases/$(database)/documents/groups/$(groupId)).data.privileges.membership.open == true;
    }
    match /users/{userId} {
      allow read, create, update, delete: if request.auth.uid == userId;
    }
		match /groupNames/{groupName} {
    	allow read;
    }
		match /groups/{groupId} {
    	allow read;
    }
		match /groups/{groupId}/members/{userId} {
    	allow read;
      allow write: if request.auth.uid == userId && groupIsOpen(groupId)
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
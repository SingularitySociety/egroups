

/users/{userId}
    created: date
    lastAccessed: date

/groupnames/{groupName}
    groupId: string

/groups/{groupId}
    created: date
    name: string
    /controls/{entityId}  (channelId, eventId, articleId)
        required: number

    /levels/{userId}
        level: number (0:owner, 1:admin, 2:mentor, 3:subscriber, 4:member, 5:guest)

    /members/{userId}
        created: date
        name: string ###
        lastAccessed: date

    /channels/{channelId}
        created: date
        name: string
        owner: userId
        control: number
        /messages/{messageId}
            created: date
            message: string
            user: {
                userId: userId
                name: sttring ###
            }
            editted: boolean
    /events/{eventId}
        title: string
        description: string
        date: date
        onwer: userId
        control: number
        /attendees/{userId}
            name: string ###
            accepted: boolean
            attended: boolean
    /articles/{articleId}
        title: string
        owner: userId
        control: number
        /authors/{userId}
            name: string ###
        /sectios/{sectionId}
            text: string
            image: string
            type: string
            updated: date
            




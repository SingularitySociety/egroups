import { should } from 'chai';
// import * as Test from 'firebase-functions-test';

// import * as ogp from "../src/utils/ogp"
// import * as ogpFunctions from "../src/functions/ogp";

should()

describe('OGP test', () => {
  
  /*
  it ('1 test', async () => {
    const url = "https://note.com/singsoc/n/n60a8080fb6cd";
    const res = await ogp.parseUrl(url);
    console.log(res);
  });

  it ('2 test', async () => {
    const url = "https://note.com/singsoc/n/n60a8080fb6cd";

    const test = Test();
    const snapshot = test.firestore.makeDocumentSnapshot({
      type: "url",
      url,
      isNew: true,
    }, `groups/123/articles/456/sections/789`);
    
    await ogpFunctions.opg_update({after: snapshot}, {});
    // const res = await ogp.parseUrl(url);
    // console.log(res);
  });
  */
});
{
  "name": "functions",
  "scripts": {
    "lint": "tslint --project tsconfig.json",
    "build": "tsc",
    "watch:tcs": "tsc -w",
    "serve": "npm run build && firebase serve --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "mocha": "cp ../firestore.rules  ./ && mocha  -r ts-node/register --recursive test/*_test.ts",
    "mocha2": "mocha  -r ts-node/register --recursive test/logger_test.ts",
    "mocha3": "mocha  -r ts-node/register --recursive test/group_function_test.ts",
    "mocha4": "mocha  -r ts-node/register --recursive test/onetime_test.ts",
    "watch:mocha": "mocha -w -watch-extensions ts -r ts-node/register --recursive test/*_test.ts"
  },
  "engines": {
    "node": "8"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@types/stripe": "^6.30.3",
    "aws-sdk": "^2.493.0",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "deepmerge": "^3.3.0",
    "express": "^4.17.1",
    "firebase-admin": "~8.1.0",
    "firebase-functions": "^3.0.1",
    "libphonenumber-js": "^1.7.20",
    "sharp": "^0.22.1",
    "stripe": "^7.3.0",
    "uuid-v4": "^0.1.0"
  },
  "devDependencies": {
    "@firebase/testing": "^0.11.0",
    "@types/chai": "^4.1.7",
    "@types/mocha": "^5.2.7",
    "chai": "^4.2.0",
    "firebase-functions-test": "^0.1.6",
    "mocha": "^6.1.4",
    "supertest": "^4.0.2",
    "ts-node": "^8.2.0",
    "tslint": "^5.12.0",
    "typescript": "^3.2.2"
  },
  "private": true
}
